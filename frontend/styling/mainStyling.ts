import {StyleSheet} from 'react-native';

const mainStyling = StyleSheet.create({
    wrapper: {
        width: '100%',
        height: '100%',
        alignItems: 'center',
    },
});

export default mainStyling;
